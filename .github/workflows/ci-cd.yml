name: CI/CD Workflow

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'

      - name: Verify Node.js Installation
        run: |
          node -v
          npm -v

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Initialize Docker Swarm
        run: |
          docker swarm init || echo "Docker Swarm is mogelijk al geïnitialiseerd"

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.4.0

      - name: Terraform Init
        run: terraform init
        working-directory: ./terraform

      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: ./terraform

      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519

      - name: Test SSH Connection
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_ed25519 swarm_user@${{ secrets.SWARM_HOST }} "echo 'SSH verbinding succesvol'"

      - name: Run Ansible Playbook
        uses: appleboy/ssh-action@v0.1.3
        with:
          host: ${{ secrets.SWARM_HOST }}
          username: swarm_user
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /home/rob/repos/infrastructure/ansible
            ansible-playbook playbook.yml -i inventory.yml

      - name: Deploy Docker Stack
        uses: appleboy/ssh-action@v0.1.3
        with:
          host: ${{ secrets.SWARM_HOST }}
          username: swarm_user
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /home/rob/repos/infrastructure
            docker stack deploy -c docker-compose.yml infrastructure_stack

