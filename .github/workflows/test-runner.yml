name: Test Self-Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: self-hosted
    steps:
      - name: Check Runner
        run: echo "De self-hosted runner is succesvol ingesteld en werkt correct!"

